/**
 * Power
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */

const m = prompt ('Введите число', 100);
const n = prompt ('Введите степень', 2);
function pow (m,n) {
  return Math.pow(m,n);
}

